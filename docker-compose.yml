services:
  postgres:
    image: postgres:18
    container_name: leadfactory-postgres
    environment:
      POSTGRES_USER: leadfactory
      POSTGRES_PASSWORD: leadfactory
      POSTGRES_DB: leadfactory
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leadfactory -d leadfactory"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:8
    container_name: leadfactory-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
  redis-data:
